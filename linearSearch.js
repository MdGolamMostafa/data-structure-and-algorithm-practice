const friendsOld = [11,33,44,65,66,67,68,69,70,71];
for (var i = 0; i < friendsOld.length; i++) {
    const old = friendsOld[i];
    if (old == 66) {
        console.log('Siam Old position is ',i,'number');
    }
}

